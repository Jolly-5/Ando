# Features

This folder is for feature-level code organization in Roblox Studio.

Conventions:
- Each feature should be its own subfolder.
- Prefer `client/`, `server/`, `shared/` subfolders inside a feature.
- Avoid cross-feature dependencies; use shared modules where necessary.

